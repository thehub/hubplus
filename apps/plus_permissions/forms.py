from django import forms
from plus_permissions.models import *
from plus_permissions.Profile import *

class PermissionSliderForm(forms.Form) :
    # get back a number of permission sliders

    #organisation = forms.CharField(max_length=100)
    #role = forms.CharField(max_length=100)
    #location = forms.CharField(max_length=100)
    #hub = forms.CharField(max_length=100)

    
