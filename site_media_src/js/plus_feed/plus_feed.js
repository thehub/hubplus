var plus_feed_ready = function () {
  jq('.feed_item_reply').each(function(i, ele) {
    setup_feed_item_reply(ele);
  });
};


var setup_feed_item_reply = function(ele) {
  var manager = jq(ele);
  
};

